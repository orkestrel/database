// The browser surface's type domain — public API contracts for browser-only
// library code (e.g. the future IndexedDBDriver, next wave). Empty for now:
// this surface has no browser-only code yet (AGENTS §5, §17.1). Types are the
// source of truth; the driver unit grows this file first.
