// Browser-test setup — environment-specific helpers loaded after `setup.ts`
// for the `src:browser` project, which runs in a real Chromium (DOM
// available, no `node:*`). Empty for now: this surface has no browser-only
// code yet (AGENTS §16.1, §17.1). The IndexedDBDriver unit grows this file
// with its own fixtures when it lands.
